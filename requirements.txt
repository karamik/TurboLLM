# Core dependencies
torch>=2.1.0
torchvision
torchaudio

# vLLM – high-performance inference engine
vllm>=0.4.0

# Transformers & tokenizers
transformers>=4.36.0
tokenizers>=0.15.0
accelerate>=0.25.0

# Quantization & FP8 support
# (vLLM uses its own FP8 kernel, but we keep these for scripts)
bitsandbytes>=0.41.0

# API & web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
sse-starlette>=1.6.0

# Utilities
numpy>=1.24.0
safetensors>=0.4.0
einops>=0.7.0

# Monitoring & logging
prometheus-client>=0.19.0
python-json-logger>=2.0.0

# For scripts (download, conversion, etc.)
requests>=2.31.0
tqdm>=4.65.0
huggingface-hub>=0.19.0

# Optional – for proprietary modules (enterprise)
redis>=5.0.0           # caching
python-jose>=3.3.0     # JWT auth
passlib>=1.7.4         # password hashing
bcrypt>=4.1.0

# For Grafana/Prometheus integration (exporter)
# (already covered by prometheus-client)
